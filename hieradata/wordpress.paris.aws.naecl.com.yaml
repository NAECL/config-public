---
# Yaml config file for wordpress.paris.aws.naecl.com server specific settings

wordpress::wordpress_version: '4.9.1'

wordpress::config::backup_bucket: 'put.name.of.aws.s3.bucket.here'

wordpress::config::sites:
  site1:
    sitename:    'test.paris.aws.naecl.com'
    dbname:      'test_wp_paris'
    sslcert:     'false'
    backup_site: 'false'

